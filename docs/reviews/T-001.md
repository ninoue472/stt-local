# レビュー: T-001 VAD ゲート

対象: `STTLocalApp/Speech/StreamingTranscriber.swift`, `STTLocalApp/State/Settings.swift`
レビュアー: Claude / 日付: 2026-06-01

## 受け入れ条件チェック

| AC | 内容 | 判定 |
|----|------|------|
| AC-A1 | 無声時に transcribe を呼ばない | ✅ `guard voiceDetected else { ... return }` で transcribe 前に分岐 |
| AC-A2 | スキップ時 lastBufferSize 据え置き + 100ms sleep | ✅ `lastBufferSize` 更新は guard 通過後。スキップ枝は sleep + return |
| AC-A3 | 閾値を Settings.silenceThreshold から取得 | ✅ |
| AC-A4 | 既存挙動を壊さない | ✅ 有声時は従来フローと同一。挿入は VAD 分岐のみ |
| Settings | silenceThreshold 追加（key/default/パターン） | ✅ `stt.silenceThreshold`, 既定 0.3, 既存と同形 |

## 良い点
- WhisperKit 純正 `AudioStreamTranscriber` の useVAD ブロックと同じセマンティクス。
- `lastBufferSize` を無声時に更新しない点が正しく実装されており、発話再開時の頭欠けを防げている。
- 影響範囲が指定 2 ファイルに収まっている。

## 指摘 / 注意
- なし（設計どおり）。
- ビルド検証は Codex 側ではサンドボックス制約で未実施 → Claude 側で別途実施。
- 手動検証（5秒無音で空 / 連続発話が途切れない）は実機確認が必要。人間の最終確認項目。

## 判定
- コードレビュー: **承認**（ビルド成功を確認後マージ可）。

# レビュー: T-029 モデルロード後のウォームアップ推論

## 対象
- 変更: `STTLocalApp/Speech/WhisperEngine.swift`, `STTLocalApp/App/AppDelegate.swift`

## 受け入れ条件の確認
- [x] `WhisperEngine.warmupTranscription(language:noSpeechThreshold:durationSeconds:)` を追加。
      約1.0s の無音 `Float` 配列を `pipe.transcribe(audioArray:decodeOptions:)` に1回流し、結果は破棄。
- [x] `startRecordingLive` を使わず自前ダミー配列方式（録音状態に触れない）。仕様の制約どおり。
- [x] `prewarmWhisper()` でモデルロード成功後・`.ready` 前に呼ぶ。失敗は `print` のみで `.ready` へ継続。
- [x] モデル切替（reloadModel → prewarmWhisper）でも同経路でウォームアップが走る。
- [x] AppState（confirmed/unconfirmed/bufferEnergy）へ影響なし。

## 設計整合
- 良好。`prewarm:true`（モデルコンパイル）に加え、実推論ホットパス（プレフィル等）を温める意図に合致。
- ウォームアップ分だけロード時間が伸びるが「読込中」表示中に吸収。許容範囲。

## 残課題 / 実機確認（人間）
- [ ] **初回発話の `[STT/timings]` 初回 pipeline が定常域（~600〜930ms）に近い**ことを `/tmp/stt.log` で確認。
- [ ] ロード時間の体感増が許容内か。
- 判定: build pass。効果はGUI実機計測で判定（ここでは未検証）。

## 結論
- コードは承認。実機計測待ち。

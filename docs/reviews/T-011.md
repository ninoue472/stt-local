# レビュー: T-011 確定/未確定テキスト＋推論中フラグの公開

実装: Codex / レビュー: Claude / 結果: **承認（Approved）**

## 受け入れ条件の確認
- [x] `AppState` に `confirmedText` / `unconfirmedText` / `isInferring` を追加。
- [x] `currentText` を computed `{ confirmedText + unconfirmedText }` に変更。
- [x] `copyableText` のコピー結果が従来と一致（`currentText` をトリムして使用、ロジック不変）。
      → 追加テスト `testCopyableTextMatchesTrimmedCurrentTextWhileRecording` で担保。
- [x] `StreamingTranscriber.updateDisplayTexts()` が confirmed/unconfirmed を別々に反映、
      境界の生スペースを保持（トリムは UI 側へ委譲）。
- [x] `isInferring` を `pipe.transcribe(...)` 直前 `true` / `defer` で `false`。
- [x] `stop()` 完了後・録音開始時に `isInferring = false` を保証（StreamingTranscriber + AppDelegate 両方）。
- [x] `AppDelegate.startRecording` のリセットを confirmedText/unconfirmedText 初期化へ更新。

## タイミング中立の確認
- 音声パイプライン（窓・`clipTimestamps`・スライド/purge・`minNewAudioSeconds`）に変更なし。
  `git diff` に短窓化等が見えるのは**先行の未コミット作業**であり T-011 由来ではない。
  Codex の実変更は isInferring / updateDisplayTexts 分割 / currentText computed 化 / AppDelegate のみ。

## 検証
- ビルド: `xcodebuild build`（`CC`/`CXX` を外して実行）→ **BUILD SUCCEEDED**。
- テスト: `xcodebuild test` → **TEST SUCCEEDED**（全11件 pass、うち新規2件）。
- `currentText` への代入残りなし（computed 化での破綻なし）。

## 備考
- ビルドには `env -u CC -u CXX` が必須（ユーザー環境の Homebrew LLVM `CC` が Xcode の
  `-index-store-path` を解さず失敗するため）。→ `docs/knowledge/build-requires-unset-cc-cxx.md` に記録。
- 次タスク T-012 はこの `confirmedText`/`unconfirmedText`/`isInferring` を消費する。

# レビュー: T-023 パネル内 言語トグルUI

担当: Codex / レビュー: Claude / 結果: **承認**

## 受け入れ条件チェック
- [x] `PanelRootView` の `MainBar` 内に言語トグル（`LanguageToggle` private View）を追加。新規ファイルなし（プロジェクト再生成不要）。
- [x] JA / KO の2値カプセルトグル。選択中はハイライトで一目で分かる。
- [x] 選択変更で `appState.language` を更新 → T-022 の `didSet` で永続化。
- [x] `appState.phase == .ready` のときのみ有効。`.recording` / `.processing` / `.downloadingModel` / `.loadingModel` では `.disabled(true)`。
- [x] HUD ダークテーマに馴染むカプセルスタイル。配置は右上ヒント行（録音ヒントの隣）で、マイク/コピー/閉じる操作と競合しない。
- [x] `PanelHeightKey`（動的リサイズ）に変更なし。
- [x] ビルド成功（`env -u CC -u CXX xcodebuild ... build` → BUILD SUCCEEDED）。

## 所見
- `help` に現在言語と「待機中のみ変更可」を表示し、無効化の理由がユーザーに伝わる配慮あり。良い。
- disabled 時もボタン自体は存在するが、コンテナ `.disabled(!isEnabled)` によりタップは発火しない。問題なし。
- ロジックは T-022 の `AppState.language` にバインドするのみで、UI 以外に影響なし。

## 残課題（実機確認）
- ja→ko→ja の切替で表示言語が実際に切り替わること。
- アプリ再起動後に最後の選択が保持されること。
- 録音中はトグルが無効（押せない）こと。

## 次
- 人間が PR をマージ後、知見化（必要なら）。

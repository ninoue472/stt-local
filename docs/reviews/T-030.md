# レビュー: T-030 先頭エッジのバジェット短縮（探索的）

## 対象
- 変更: `STTLocalApp/Speech/StreamingTranscriber.swift`, `docs/knowledge/realtime-budget-and-whisper-offline-limit.md`

## 受け入れ条件の確認
- [x] budget を `steadyMinNewAudioSeconds=1.5s` / `initialMinNewAudioSeconds=0.7s` に分離。
- [x] `usesShortInitialBudget` フラグ。録音開始直後のみ 0.7s、初回確定（`lastConfirmedSegmentEndSeconds>0`）
      または総バッファ 1.5s 到達で 1.5s 固定へ復帰（`shouldRestoreSteadyBudget`）。`resetState` で true に戻す。
- [x] 適応budget（EMA/ピーク追従）は**再導入していない**。単純な「先頭一時短縮→固定復帰」のみ。T-027 教訓を順守。
- [x] 確定/未確定ロジック・無損失スライド（`slideWindowIfNeeded`）は不変。最終テキストへの影響なし。

## 設計整合
- 良好。窓がまだ短く decode が軽い録音開始直後だけ第一表示を前倒しする局所策。常時短縮しないため
  T-027 の周期的バックログを持ち込まない。

## 残課題 / 実機計測（人間）— ※採否は計測で決める探索的タスク
- [ ] `/tmp/stt.log` の `[STT/timings]` で、先頭の `budget=0.7s` 区間でも `pipeline` が継続的に
      budget 超過しない（バックログ悪化しない）ことを確認。
- [ ] 第一表示が体感で速くなるか。悪化する環境があれば短縮幅を保守化、または不採用。
- 判定: build pass。効果・副作用はGUI実機計測で判定（ここでは未検証）。

## 結論
- コードは承認（実装として妥当）。**採否は実機計測の結果次第**。不採用なら理由を knowledge へ記録。

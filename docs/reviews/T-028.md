# レビュー: T-028 マイク権限の起動時先行取得＋波形ベースライン即時表示

## 対象
- ブランチ: `fix/first-launch-waveform-and-latency`
- 変更: `STTLocalApp/App/AppDelegate.swift`, `STTLocalApp/Speech/StreamingTranscriber.swift`

## 受け入れ条件の確認
- [x] 権限要求を起動時（`prewarmWhisper()` 冒頭）に1回先行実行。`Task { _ = await AudioProcessor.requestRecordPermission() }`
      の fire-and-forget で、モデルロードをブロックしない。
- [x] 録音時の権限チェック（`StreamingTranscriber.start()`）は残置 → 拒否時の既存エラー文言を維持。
- [x] 既に許可済みなら副作用なし（OSの権限要求は許可済みなら即返る）。
- [x] `startRecording()` で `phase=.recording` の直前に `bufferEnergy=[0]` を同期設定。`await` 前なので
      初回ダイアログ待ち中も波形ベースライン枠が出る。`start()` 側の `[0]` 設定との二重も無害。
- [x] 波形のタップコールバック駆動（入力モニタ）設計は不変。

## 設計整合
- 良好。権限取得タイミングを「録音時」→「起動時」へ前倒しし、初回セッションの
  「権限確定直後の audioProcessor 起動競合」を回避する狙いに合致。最小差分。

## 残課題 / 実機確認（人間）
- [ ] **初回起動（未許可状態）で録音 → 波形が最初から出る**ことを実機確認。
- [ ] 権限拒否時に従来どおりエラー表示へ落ちること。
- 判定: build pass。挙動はGUI実機確認が必須（ここでは未検証）。

## 結論
- コードは承認。実機確認待ち。

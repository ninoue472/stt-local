# レビュー: T-002 幻聴ポストフィルタ

対象: `STTLocalApp/Speech/HallucinationFilter.swift`（新規）, `STTLocalApp/Speech/StreamingTranscriber.swift`
レビュアー: Claude / 日付: 2026-06-01

## 受け入れ条件チェック

| AC | 内容 | 判定 |
|----|------|------|
| AC-B1 | blocklist に既知 4 フレーズを含む | ✅ `blockedPhrases` に 4 句 |
| AC-B2 | 完全一致 + (noSpeechProb>=0.4 or avgLogprob<=-0.7) で除外 | ✅ `isHallucination` |
| AC-B3 | 信頼度ゲート未達なら除外しない（実発話保護） | ✅ guard で blocklist 一致でも閾値未満は false |
| AC-B4 | フィルタを updateSegments の前に適用 | ✅ L127-128 で filter 後に updateSegments |
| 制約 | 完全一致のみ | ✅ `Set.contains` 完全一致 |
| 制約 | 閾値を定数集約 | ✅ `noSpeechProbThreshold` / `avgLogprobThreshold` |

## 良い点
- `normalize` を unicodeScalars ベースで whitespace/punctuation/symbols 除去。
  CJK 句点「。、」全角「！？」はいずれも Unicode punctuation(Po) のため `punctuationCharacters` で除去される。
- 純粋関数（enum static のみ・副作用なし）で T-003 のテストに乗せやすい。
- StreamingTranscriber の変更は 1 行（セグメント取得行）に収まり、確定処理前に適用されている。

## 指摘 / 注意
- 軽微: `normalize` は記号も全除去するため、blocklist に記号を含む語を将来追加する場合は注意（現状の 4 句は問題なし）。
- 閾値 0.4 / -0.7 は経験則。実機ログで誤除去/取りこぼしが出たら調整（Settings 化は将来検討）。
- 単体テストは T-003 に分離（リポジトリに XCTest ターゲットが無いため）。

## 判定
- コードレビュー: **承認**（ビルド成功を確認後マージ可）。

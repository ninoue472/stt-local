# レビュー: T-003 XCTest ハーネス整備 + 単体テスト

対象: `generate_project.rb`, `STTLocalAppTests/HallucinationFilterTests.swift`
レビュアー: Claude / 日付: 2026-06-01

## 受け入れ条件チェック

| AC | 内容 | 判定 |
|----|------|------|
| テストターゲット追加 | unit_test_bundle + 再生成 | ✅ `:unit_test_bundle`, `add_dependency`, TEST_HOST/BUNDLE_LOADER, Debug に ENABLE_TESTABILITY |
| 共有スキーム | xcodebuild test が動く | ✅ `XCScheme.configure_with_targets(...)` + `save_as(..., true)` |
| 各テストケース | 4 シナリオ + normalize | ✅ 5 メソッドで網羅 |
| `xcodebuild test` pass | — | ✅ **Executed 5 tests, 0 failures / TEST SUCCEEDED** |
| 本体ビルド非破壊 | アプリ起動も従来どおり | ✅ launch_target は app、test 追加のみ |

## 良い点
- 閾値の境界値（`==` 閾値）でテストしており、`>=` / `<=` の境界挙動を正しく検証。
- 実発話保護ケース（低 noSpeechProb + 高 avgLogprob → 残る）を明示的にカバー。
- `makeSegment` ヘルパで WhisperKit `TranscriptionSegment` のデフォルト引数を活用、簡潔。

## 指摘 / 注意
- なし。
- Codex は Package.resolved 保全処理も追加（再生成で SPM 解決が飛ばないように）。副作用は確認済みで問題なし。

## 判定
- **承認**。T-001/T-002/T-003 すべて完了。

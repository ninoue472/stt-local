# レビュー: T-024 言語選択UIをトグル→ドロップダウンに変更

担当: Codex / レビュー: Claude / 結果: **承認**

## 受け入れ条件チェック
- [x] 言語UIが `Menu` ベースのドロップダウン（`LanguageMenu`）。トグル/セグメントを廃止。
- [x] 選択肢は「日本語」「한국어」のフル表記。現在選択中の言語をラベルに表示（chevron 付き）。
- [x] 現在言語の項目に checkmark（`Label(.., systemImage: "checkmark")`）。
- [x] 選択で `appState.language`（"ja"/"ko"）更新 → 既存の永続化経路。
- [x] `appState.phase == .ready` のときのみ操作可、それ以外は `disabled`。
- [x] 旧 `LanguageToggle`（カプセル2値）の残骸なし（`LanguageMenu` へ完全置換）。
- [x] `PanelRootView.swift` のみ変更、新規ファイルなし。
- [x] ビルド成功（`env -u CC -u CXX xcodebuild ... build` → BUILD SUCCEEDED）。

## 所見
- `help` と無効化（待機中のみ変更可）の配慮は T-023 から維持。
- `.menuStyle(.borderlessButton)` + `fixedSize()` で HUD に収まるコンパクトなドロップダウン。
- ロジック層（AppState/Settings/StreamingTranscriber/DecodingPresets）は不変。要件どおり UI のみ。

## 残課題（実機確認・人手）
- ドロップダウンから ja↔ko 切替が反映されること。
- 録音中はドロップダウンが無効なこと。
- 見た目（HUD への収まり）。

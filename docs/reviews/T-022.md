# レビュー: T-022 言語切替の基盤

担当: Codex / レビュー: Claude / 結果: **承認**

## 受け入れ条件チェック
- [x] `DecodingPresets.streaming(language:noSpeechThreshold:)` に変更。`ja` で従来と同一 `DecodingOptions`（他フィールドは不変、`language` のみ引数化）。
- [x] `StreamingTranscriber` が `start()` で `Settings.shared.language` をスナップショット（`language` フィールド、`resetState()` 直後に代入）。`transcribeStep` は保持値を使用 → 録音中の Settings 変更に影響されない。
- [x] フィルタ適用は `language == "ja"` のときのみ。`ko` は raw セグメント素通し（`StreamingTranscriber.swift:172`）。
- [x] `AppState.language` 追加。初期値 `Settings.shared.language`、`didSet` で永続化。
- [x] `WhisperEngine` / `AppDelegate` 不変（モデルロード経路に変更なし）。
- [x] `HallucinationFilterTests` 含む全10テスト pass。
- [x] ビルド成功（`env -u CC -u CXX xcodebuild ... build` → BUILD SUCCEEDED）。

## 設計整合
- 「言語は録音セッション境界で確定」を `start()` スナップショットで正しく実装。混在防止の要件を満たす。
- 触ってはいけない領域（タイミング系・モデルロード）に変更なし。

## 所見
- 低リスク。純粋な設定追加で並行性・タイミングへ影響なし。
- ビルドは Codex の sandbox では SwiftPM の一時ファイル作成不可で失敗したが、Claude 側の実機環境で build/test とも成功を確認。
- `pbxproj`/`xcscheme` の差分はプロジェクト生成ツールの識別子 churn（先行差分）であり T-022 とは無関係。PR には含めない。

## 次
- T-023（パネル内 言語トグルUI）へ進む。`AppState.language` バインド前提が満たされた。

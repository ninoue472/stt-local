# レビュー: T-012 確定/未確定の色分け表示 ＋ 推論ステータスバッジ

実装: Codex / レビュー: Claude / 結果: **承認（Approved、実機目視は保留）**

## 受け入れ条件の確認
- [x] `TranscriptView` が確定=`.primary`、未確定=`Color.white.opacity(0.45)` の Text 連結で表示。
- [x] 未確定末尾に `…`（録音中のみ、`.ready` では付けない）。
- [x] 未確定→確定の昇格で色が薄→濃へ（confirmed/unconfirmed を別 Text で描くため自然に変化）。
- [x] `.ready` は `lastFinalText` を全文 `.primary`。
- [x] `StatusBadge` を `MainBar` 左カラム・`WaveformView` 直上に配置。
  - [x] `.recording`+`isInferring` / `.processing` → 脈動ドット＋「推論中…」
  - [x] `.recording`+`!isInferring` → 静的ドット＋「聞いています」
  - [x] `.ready`/その他 → 非表示
- [x] 脈動は `isInferring`/`phase` の onChange に同期（`repeatForever`、false で停止）。
- [x] 表示トリムは「全体先頭の空白除去」のみ。確定↔未確定の境界スペースは
      `trimLeadingWhitespace` で確定側→未確定側の順に削り、境界を壊さない。

## 検証
- ビルド: `env -u CC -u CXX xcodebuild build` → **BUILD SUCCEEDED**。
- 新規 `StatusBadge.swift` 追加に伴い `generate_project.rb` 実行済み（pbxproj 再生成）。
- 既存 `StatusIcon` は不変。StreamingTranscriber/音声パイプラインに変更なし。

## 保留（人間の実機確認推奨）
- 未確定色 `opacity(0.45)` の視認性（薄すぎ/濃すぎ）。実機で微調整余地あり。
- バッジ脈動の体感速度（`duration: 0.9`）。

## 備考
- `displayText`（スクロール追従の onChange キー）と `visibleText`（描画用）が併存。
  機能上の問題はないが、将来整理可。
